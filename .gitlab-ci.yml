image: python:3.8.5

stages:
  - build
  - test

before_script:
  - pip install -r ./install/requirements.txt
  - apt-get update
  - apt-get install -y ffmpeg

build:
  stage: build
  script:
    - pip install ./src/

test:
  stage: test
  script:
    - cd ./tests
    - python -m unittest
