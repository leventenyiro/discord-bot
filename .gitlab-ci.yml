image: python:latest

stages:
  - build
  - test

before_script:
  - pip install -r ./install/requirements.txt

build:
  stage: build
  script:
    - pip install ./src/

test:
  stage: test
  script:
    - cd ./tests
    - python -m unittest
